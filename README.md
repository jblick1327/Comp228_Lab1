I chose to overload the Employee constructor rather than the bonus calculator itself. This way the employeeCount variable can properly be counted, with all calculations being handled upon object initialization. If it were to be updated, it should have getters and setters that enforce this behaviour upon changes, but I think this works for now.

<img width="976" height="256" alt="OutputScreenshot" src="https://github.com/user-attachments/assets/ecd802ea-2faa-4ab0-9f70-ed9eb316821d" />
